---
import EventDirectoryPage from "../../components/EventDirectoryPage.astro";
import { getEventDirectoryPage } from "../../lib/event-directory.js";

const directoryPage = await getEventDirectoryPage(null, 1);
---

<EventDirectoryPage {...directoryPage} />
