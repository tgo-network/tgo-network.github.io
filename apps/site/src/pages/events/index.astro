---
import CollectionCard from "../../components/CollectionCard.astro";
import BaseLayout from "../../layouts/BaseLayout.astro";
import { formatDateRange, formatRegistrationState } from "../../lib/format";
import { listEvents } from "../../lib/public-api";

const events = await listEvents();
---

<BaseLayout title="活动 | TGO Network" description="支持报名状态的已发布活动页面。">
  <main class="page-shell page-stack">
    <section class="hero">
      <div class="hero-card">
        <span class="eyebrow">公开路由</span>
        <h1>活动</h1>
        <p>
          活动页面已经可以展示报名状态、场地信息与议程内容，同时继续保持与静态优先 Astro 交付模型兼容。
        </p>
      </div>
    </section>

    <section class="section">
      <div class="grid grid-2">
        {
          events.map((event) => (
            <CollectionCard
              href={`/events/${event.slug}`}
              label={event.city.name}
              title={event.title}
              summary={event.summary}
              image={event.coverImage}
              meta={[formatDateRange(event.startsAt, event.endsAt), event.venueName, formatRegistrationState(event.registrationState)]}
            />
          ))
        }
      </div>
    </section>
  </main>
</BaseLayout>
